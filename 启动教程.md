# IM 项目本地启动教程

本文档按当前已验证成功的方式启动：Docker 只启动 MongoDB，本地命令启动后端和前端。

## 1. 启动 MongoDB

打开 PowerShell：

```powershell
cd C:\Users\zlf\Desktop\1111\IM_backend\pure_im_backend
docker compose up -d mongodb
```

确认 MongoDB 已启动：

```powershell
docker ps --filter "name=pure-im-mongodb"
```

看到类似下面内容即可：

```text
pure-im-mongodb   Up ... (healthy)   0.0.0.0:27017->27017/tcp
```

也可以测试 MongoDB：

```powershell
docker exec pure-im-mongodb mongosh --quiet --eval "db.adminCommand('ping')"
```

返回：

```text
{ ok: 1 }
```

## 2. 启动后端

新开一个 PowerShell，执行：

```powershell
cd C:\Users\zlf\Desktop\1111\IM_backend\pure_im_backend
conda activate learn
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

启动成功会看到：

```text
Application startup complete.
Uvicorn running on http://127.0.0.1:8000
```

保持这个窗口不要关闭。

测试后端：

```powershell
Invoke-WebRequest http://127.0.0.1:8000/api/auth/confirm
```

返回 `200` 表示后端可用。

## 3. 启动前端

再新开一个 PowerShell，执行：

```powershell
cd C:\Users\zlf\Desktop\1111\IM_front\front_IM
npm run dev
```

启动成功会看到：

```text
Local: http://localhost:5173/
```

浏览器打开：

```text
http://127.0.0.1:5173/
```

点击页面里的“检测后端”，应显示“后端可用”。

## 4. 当前前端后端地址

前端接口配置在：

```text
C:\Users\zlf\Desktop\1111\IM_front\front_IM\src\services\api.ts
```

当前应为：

```ts
export const API_BASE = import.meta.env.VITE_API_BASE || "http://127.0.0.1:8000";
export const WS_BASE = import.meta.env.VITE_WS_BASE || "ws://127.0.0.1:8000";
```

## 5. 常见问题

### 后端提示 27017 无法连接

说明 MongoDB 没启动。执行：

```powershell
cd C:\Users\zlf\Desktop\1111\IM_backend\pure_im_backend
docker compose up -d mongodb
```

### 后端提示 8000 端口被占用

查看是谁占用：

```powershell
Get-NetTCPConnection -State Listen | Where-Object { $_.LocalPort -eq 8000 }
```

查看对应进程：

```powershell
$pids = @(Get-NetTCPConnection -State Listen | Where-Object { $_.LocalPort -eq 8000 } | Select-Object -ExpandProperty OwningProcess -Unique)
Get-CimInstance Win32_Process | Where-Object { $pids -contains $_.ProcessId } | Select-Object ProcessId,Name,CommandLine
```

如果确认是旧后端进程，可以停止：

```powershell
Stop-Process -Id 进程ID
```

### 前端显示“后端不可用”

依次检查：

```powershell
Invoke-WebRequest http://127.0.0.1:8000/api/auth/confirm
```

如果这里不是 `200`，说明后端没有正常启动。

如果这里是 `200`，但页面仍显示不可用，检查前端是否启动的是本地 Vite：

```powershell
Invoke-WebRequest http://127.0.0.1:5173/
```

还可以强刷浏览器：

```text
Ctrl + F5
```

### 避免 Docker 旧容器占用端口

查看 Docker 容器：

```powershell
docker ps -a
```

如果有不相关容器占用了 `8000` 或 `5173`，可以停止，例如：

```powershell
docker stop sleepapi
docker stop pure-im-frontend
```

## 6. 关闭服务

关闭前端：在前端 PowerShell 窗口按：

```text
Ctrl + C
```

关闭后端：在后端 PowerShell 窗口按：

```text
Ctrl + C
```

关闭 MongoDB：

```powershell
cd C:\Users\zlf\Desktop\1111\IM_backend\pure_im_backend
docker compose down
```

## 7. 推荐启动顺序

每次开发时按这个顺序：

```text
1. docker compose up -d mongodb
2. python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
3. npm run dev
4. 打开 http://127.0.0.1:5173/
```
